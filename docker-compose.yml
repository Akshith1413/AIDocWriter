services:
  api:
    build: ./backend
    env_file:
      - .env
    volumes:
      - aureview_data:/data
    restart: unless-stopped

  web:
    build: ./frontend
    depends_on:
      - api
    ports:
      - "8080:80"
    restart: unless-stopped

volumes:
  aureview_data:
